@model IEnumerable<QuizApp.Models.Question>

@{
    ViewBag.Title = "List of Questions";
}

<div class="row row-offcanvas row-offcanvas-right">
    <div class="col-xs-12">
        <div class="jumbotron text-center">
            <h1>Questions Page</h1>
        </div>
    </div>
    <div class="col-xs-12">
        @Html.Partial("_Menu")
        <div class="col-sm-10">
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.TypeQuestion.Type)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Title)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CorrectAnswer)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.TypeQuestion.Type)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Title)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CorrectAnswer)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.QuestionID }, new { @class = "btnEdit" }) |
                            @Html.ActionLink("Details", "Details", new { id = item.QuestionID }, new { @class = "btnView" }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.QuestionID }, new { @class = "btnDel" })
                        </td>
                    </tr>
                }

            </table>
            <p>
                @Html.ActionLink(" Add Question ", "Create", null, new { @class = "btn btn-primary btn-sm;glyphicon glyphicon-plus" })
            </p>

        </div><!--/row-->
    </div><!--/.col-xs-12.col-sm-9-->

</div>